import React, { useState } from 'react'
import { useAdminStore } from '../store/adminStore'
import { toast } from 'react-hot-toast'
import { Button, Card, CardContent, CardHeader, CardTitle, Dialog, DialogContent, DialogHeader, DialogTitle, Input, Label, Switch, Textarea } from '../components/ui'
import { Edit, Trash2 } from 'lucide-react'

export function ShippingMethodSettings() {
  // ... (copy the entire ShippingMethodSettings component here)
}